{
  "name": "sitetree-gen",
  "version": "1.2.1",
  "description": "Sitetree Generator.",
  "main": "./index.js",
  "bin": {
    "sitetree-gen": "./bin/sitetree-gen"
  },
  "scripts": {
    "test": "ava --verbose",
    "check": "eslint .",
    "git-ignore": "git rm -r --cached . && git add -A && git commit --allow-empty -m 'gitignore-refresh' && git reset --soft HEAD^",
    "git-commit": "git add -A && git commit",
    "git-push": "git push origin master",
    "npm-publish": "npm publish"
  },
  "keywords": [
    "sitetree"
  ],
  "engines": {
    "node": ">=6.11.4"
  },
  "author": "isaxxx",
  "repository": {
    "type": "git",
    "url": "https://github.com/isaxxx/sitetree-gen"
  },
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.4.1",
    "fs-extra": "^6.0.0",
    "puppeteer": "^1.11.0",
    "update-notifier": "^2.5.0",
    "yargs": "^11.0.0"
  },
  "devDependencies": {
    "ava": "^0.25.0",
    "babel-eslint": "^10.0.1",
    "eslint": "^5.3.0"
  }
}
